# Claude Instructions For X-TODO

Read `AGENTS.md` first. It is the canonical repo instruction file.

Claude-specific reminders:

- Do not treat a Linux or WSL source check as proof of Windows UI behavior.
- Before editing, read the relevant `docs/conventions/*` file for the area.
- Do not commit or alter `VERSION` unless the user explicitly asks for a release or version change.
- Keep long-lived lessons in `docs/conventions/`. Do not recreate `docs/tasks/` for completed work.
